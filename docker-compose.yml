services:
    remix:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: remix-app
        working_dir: /app
        volumes:
            - .:/app
        ports:
            - '80:3000'
        env_file:
            - .env
        depends_on:
            - db
        networks:
            - remix-net
        command: ['pnpm', 'start']

    db:
        image: postgres:15
        container_name: remix-db
        restart: always
        env_file:
            - .env
        ports:
            - '5432:5432'
        volumes:
            - pgdata:/var/lib/postgresql/data
        networks:
            - remix-net

volumes:
    pgdata:

networks:
    remix-net:
